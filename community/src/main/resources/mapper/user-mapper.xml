<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace 写的是mapper为哪个服务-->
<mapper namespace="com.normalUniversity.community.dao.UserMapper">
    <!--替换select后面的，要用sql标签-->
    <sql id = "insertFields">
          username, password, salt, email, type, status, header_url, create_time
    </sql>
    <sql id = "selectFields">
        id, username, password, salt, email, type, status, header_url, create_time
    </sql>
    <!--resultType前面不写包名这些是因为properties配置了entity
                #{id}是传入selectById的参数-->
    <select id="selectById" resultType="User">
        select <include refid="selectFields"></include>
        from user
        where id = #{id}
    </select>

    <select id="selectByUsername" resultType="User">
        select <include refid="selectFields"></include>
        from user
        where username = #{username}
    </select>

    <select id="selectByEmail" resultType="User">
        select  <include refid="selectFields"></include>
        from user
        where email = #{email}
    </select>
    <!--复杂类型需要申明参数 parameterType，简单不需要 -->
    <insert id="insertUser" parameterType="User" keyProperty="id">
        insert into user (<include refid="insertFields"></include>)
        values(#{username},#{password},#{salt},#{email},#{type},#{status},#{headerUrl},#{createTime})
    </insert>

    <update id="updateStatus">
        update user set status=#{status} where id=#{id}
    </update>
    <update id="updatePassword">
        update user set password=#{password} where id=#{id}
    </update>

</mapper>
